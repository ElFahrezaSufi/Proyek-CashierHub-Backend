# ========================================
# INSTRUKSI PENTING:
# ========================================
# 1. Salin file ini menjadi `.env` (tanpa .example)
# 2. Isi nilai-nilai di bawah dengan kredensial Neon PostgreSQL Anda
# 3. Jangan commit file `.env` ke git (sudah ada di .gitignore)
# ========================================

# DATABASE CONFIGURATION (dari Neon Dashboard)
# ---------------------------------------------
# Connection String lengkap dari Neon (tab "Connection Details")
# Format: postgresql://[user]:[password]@[host]/[database]?sslmode=require
DATABASE_URL=

# Detail koneksi individual (ambil dari connection string di atas)
PGUSER=your_postgres_user
PGHOST=your_host_from_neon
PGDATABASE=your_database_name
PGPASSWORD=your_postgres_password
PGPORT=5432

# SERVER CONFIGURATION
# ---------------------------------------------
PORT=3000

# ========================================
# CONTOH PENGISIAN (JANGAN DIPAKAI):
# ========================================
# DATABASE_URL=postgresql://myuser:mypass123@ep-xxx-123456.us-east-2.aws.neon.tech/cashierhub?sslmode=require
# PGUSER=myuser
# PGHOST=ep-xxx-123456.us-east-2.aws.neon.tech
# PGDATABASE=cashierhub
# PGPASSWORD=mypass123
# PGPORT=5432
# PORT=3000
